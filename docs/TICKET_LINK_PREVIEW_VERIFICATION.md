# チケット連携（PR2）Preview 動作確認手順

このリポジトリには Playwright / Cypress / Storybook / jsdom / testing-library のいずれも無く、
**ブラウザ自動テスト基盤が存在しない**（`vitest` の `environment` は `node`）。
そのため UI は API・純関数テストでカバーし、画面の確認は本手順の**手動確認**で行う。

環境の構築手順（Preview 専用 DB / 検証用 OA・LINE Login・LIFF の作成）は
[`TICKET_LINK_VERIFICATION_ENVIRONMENT.md`](./TICKET_LINK_VERIFICATION_ENVIRONMENT.md) を参照。
本書は「用意できた環境で何を確認するか」だけを扱う。

確認は **ご本人が Preview へログインして**実施してください。
パスワード・セッショントークン・Cookie を第三者や AI へ共有する必要はありません（要求もしません）。

---

## 0. 前提

### DB migration（適用済み）

| migration | 状態 |
|---|---|
| `20260801000000_add_ticket_link_models` | **適用済み**（2026-07-31 16:48Z） |
| `20260801000001_add_ticket_link_manual_flow` | **適用済み**（2026-07-31 16:48Z） |

Preview は Production と同一 DB を参照する（Preview から本番作品 ID を解決できることで実証済み）。
したがって Preview で本フローを実行できる。

> ⚠️ **バックアップに関する注意（重要）**
> 上記 migration は、Supabase ダッシュボード上の PITR・自動バックアップの利用可否を**未確認のまま**、
> DB 内部の WAL アーカイブ稼働のみを確認して適用した。事前ゲートを完全には満たしていない。
> **今後の Production DB 変更前には、Supabase ダッシュボードでバックアップ・復旧設定の確認を必須とする。**

### 検証データの条件

実在の購入者・本番予約情報は使わない。検証専用 Work と検証用 LINE アカウントを使い、架空値を用いる。

```
名前      ：動作確認ユーザー
予約番号   ：999-001
コードネーム：TEST-AGENT-1
```

---

## 1. 管理画面での準備

URL: `{PreviewURL}/oas/{oaId}/works/{workId}/liff?tab=ticket_link`

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | タブを開く | 「チケット連携」タブが表示される |
| 2 | 初期状態 | 帯が**橙色**で「プレイヤーにはまだ公開されません」＋不足理由が箇条書き |
| 3 | 「チケット連携ページを作成」 | 「チケット連携ページを作成しました（非公開の下書きです）」。ボタンが消え、状態「非公開（下書き）」が出る |
| 4 | もう一度タブを開き直す | **作成ボタンは出ない**（冪等・重複作成されない） |
| 5 | 「チケット連携を有効にする」ON | チェックが入り「未保存の変更があります」が出る |
| 6 | 「手動入力を有効にする」ON | 同上 |
| 7 | スクリーンショット登録 | **操作できない**（disabled）＋「準備中」表記 |
| 8 | 「チケット種別を追加」×3 | 空行が3行増える |
| 9 | `single` / `1名チケット` / `1` / 有効<br>`pair` / `2名グループチケット` / `2` / 有効<br>`quad` / `4名グループチケット` / `4` / 有効 | 入力できる |
| 10 | ↑↓ で並び替え | 行の順序が入れ替わる |
| 11 | 2行目のキーを `single` に変えて保存 | 該当行の下に**「キーが重複しています。」** |
| 12 | 人数を `0` にして保存 | 該当行に「参加人数は1〜20の整数で入力してください。」 |
| 13 | 人数を `21` にして保存 | 同上 |
| 14 | 11〜13 を元に戻す | 入力内容が消えていない |
| 15 | 登録完了メッセージを入力 | 入力できる |
| 16 | ボタンラベル `運営へ報告` | ラベル欄の下に「この文字は LINE へ送信されません」 |
| 17 | 送信メッセージを空にする | **保存ボタンが無効**＋「送信メッセージが空のため、報告ボタンを有効にできません。」 |
| 18 | 送信メッセージ `チケット連携の動作確認が完了しました` | 保存ボタンが有効に戻る |
| 19 | 「設定を保存」 | トースト**「設定を保存しました」**、「未保存の変更があります」が消える |
| 20 | ページを再読込 | 設定が**復元される**（種別3件・並び順・文言すべて） |
| 21 | ホーム/アンケートタブを確認 | **既存の LIFF・survey 設定が消えていない** |
| 22 | 「公開する」 | 状態が「公開中」になる |
| 23 | 帯を確認 | **緑色**で「プレイヤーへの公開準備が完了しています」 |
| 24 | 未保存のまま別ページへ移動 | ブラウザの離脱警告が出る |

### 権限確認

| 操作 | 期待結果 |
|---|---|
| viewer 権限で開く | 入力・保存がすべて disabled |
| tester 以上で開く | 保存できる |
| 権限外 Work の ID を URL に入れる | 403 または 404（更新できない） |

※ 実在ユーザーの権限は変更しないこと。

---

## 2. LIFF 実機確認

対象 OA を**友だち追加済み**の検証用 LINE アカウントで行う（友だちでないと 403）。
Preview を LIFF として開くには、Preview を指す**検証用 LIFF アプリ**が必要。
本番 LIFF の endpoint URL・本番 OA 設定は変更しないこと。

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | LIFF HOME `/liff/w/{workPublicId}` を開く | 「チケット連携」カードが出る |
| 2 | カードを開く | 「手動で入力」と「スクリーンショットから登録（準備中）」 |
| 3 | 「スクリーンショットから登録」を押す | **押せない**（disabled） |
| 4 | 「手動で入力」 | 入力画面。対象公演は作品名で**固定表示** |
| 5 | チケット種別を選択 | 3件が設定順で出る（1件だけなら固定表示） |
| 6 | 名前 `動作確認ユーザー` | 入力できる |
| 7 | 予約番号 `abc` で進む | 「予約番号の形式が正しくありません。」 |
| 8 | 予約番号 `999-001` で進む | 確認画面へ |
| 9 | 確認画面 | 予約番号が**全桁**表示。公演・日時・種別・名前が出る。日時は「運営確認後に反映されます」 |
| 10 | 「戻って修正する」 | 入力内容が**保持**されている |
| 11 | 「この内容で進む」 | コードネーム欄が**参加人数分**（1/2/4名で件数が変わる） |
| 12 | 1件空欄のまま進む | 「コードネームを入力してください。」 |
| 13 | すべて入力して進む | 最終確認に公演・日時・種別・予約番号・コードネーム一覧 |
| 14 | 「この内容で登録」を**連打** | **1件だけ**登録される（二重登録なし） |
| 15 | 完了画面 | 「チケット連携を受け付けました」＋案内文＋連携状態**「運営確認待ち」** |
| 16 | タブを開き直す | 連携済み一覧に出る。予約番号が**`999-***`** にマスク |
| 17 | 既存登録がある状態 | 「別のチケットを連携する」導線が**残っている** |
| 18 | 同じ予約番号で再登録 | 既存が返り二重作成されない |
| 19 | 別の予約番号 `999-002` で登録 | **別レコード**として登録できる |
| 20 | 入力途中で閉じて開き直す | 「入力途中の情報を再開する」「最初からやり直す」 |
| 21 | 「最初からやり直す」 | 入力が消えて未登録状態へ |

### 報告ボタン

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 完了画面のボタン表示 | **「運営へ報告」**（＝ラベル） |
| 2 | 1回タップ | トークへ**「チケット連携の動作確認が完了しました」**が**1通だけ**届く |
| 3 | 届いた本文を確認 | **「運営へ報告」は送信されていない** |
| 4 | 連打 | 2通目が送られない（ボタンが無効化） |
| 5 | 送信後 | 「送信しました。」表示 → LIFF が閉じる |
| 6 | LIFF 外ブラウザで完了画面を開きボタンを押す | 「チケット連携は完了しています。LINEへの報告メッセージのみ送信できませんでした。…」が出て、**登録は完了扱いのまま** |

### 異常系

| 操作 | 期待結果 |
|---|---|
| 対象 OA を友だち追加していないアカウント | 「公式アカウントを友だち追加してください」 |
| チケット連携を無効にして開く | 「この機能は現在ご利用いただけません。」 |
| ticket_link ページを非公開にして開く | カードが出ない |
| チケット種別をすべて無効にする | 手動入力導線が出ない |
| 別 LINE アカウントで同じ予約番号 `999-001` | **「この予約番号は現在確認が必要な状態です。運営からの案内をお待ちください。」**<br>先行登録者の名前・コードネーム・登録日時が**一切出ないこと** |

※ 2つ目の検証用 LINE アカウントが無い場合、最後の1項目は「未確認」で構いません。

---

## 3. 失敗したときに取得してほしい情報

うまくいかない項目があれば、以下を控えてください。

- 表示された画面（どのタブ・どのステップか）
- 画面に出たエラーメッセージ（そのままの文言）
- 操作していた URL（**クエリの token 類は除く**）
- 操作直前の設定内容（チケット種別のキー・人数・有効状態など）
- 発生時刻（できれば秒まで）
- ブラウザの開発者ツール → Network で失敗したリクエストの**パスと HTTP status**
- そのレスポンス JSON の **`error.code` のみ**（例 `TICKET_LINK_CONFLICT`）

### 記録しないでください

- パスワード / セッショントークン / Cookie / `accessToken`
- API キー
- レスポンス JSON の全文
- 実在の予約番号・本名・メールアドレス
- LINE userId の全体（必要なら末尾4文字のみ）

---

## 4. 確認結果の記録

```
Preview deploy        ：成功（SHA: 2045dff）
Preview DB migration  ：適用済み
管理画面 設定保存      ：確認済み／未確認
ticket_link ページ公開 ：確認済み／未確認
LIFF画面表示          ：確認済み／未確認
チケット確定API        ：確認済み／未確認
LINE sendMessages     ：確認済み／未確認
別ユーザー競合         ：確認済み／未確認
```
