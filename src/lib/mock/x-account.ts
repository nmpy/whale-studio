/**
 * X アカウント — 固定データ & 手動更新データ
 *
 * ─────────────────────────────────────────────────────────
 * ① 固定データ（X_PROFILE）
 *    アカウント情報が変わったときだけ編集する。
 *
 * ② 手動更新データ（X_MANUAL_STATS）
 *    フォロワー数など定期的に変化する数値。
 *    数値を確認したら updatedAt と一緒に書き換える。
 *    将来: X API または X アーカイブで自動化予定。
 * ─────────────────────────────────────────────────────────
 */

import type { XProfile, XManualStats } from '@/lib/types/x';

// ─────────────────────────────────────────────────────────
// ① 固定データ
// ─────────────────────────────────────────────────────────

export const X_PROFILE: XProfile = {
  displayName: 'nmpy',
  username:    'nmpy_jp',
  profileUrl:  'https://x.com/nmpy_jp',
  bio:         '個人開発 / Next.js / TypeScript / LINE Bot / 謎解き',
  // avatarUrl: 'https://example.com/avatar.jpg',  // 画像URLが決まったらコメントを外す
};

// ─────────────────────────────────────────────────────────
// ② 手動更新データ
// ★ 定期的にここを書き換えて更新する
// ─────────────────────────────────────────────────────────

export const X_MANUAL_STATS: XManualStats = {
  // ── フォロワー関連 ──────────────────────────────────────
  followersCount:      1543,   // ← フォロワー数を確認して書き換える
  followersDayChange:  +5,     // ← 前日比（正: 増加 / 負: 減少）
  followersWeekChange: +23,    // ← 7日間の増減

  // ── 基本情報 ─────────────────────────────────────────────
  followingCount:      312,    // ← フォロー中
  totalPostCount:      2847,   // ← 累計ポスト数

  // ── 今月の集計 ───────────────────────────────────────────
  monthlyPostCount:    47,     // ← 今月の投稿数
  monthlyLikeTotal:   1840,    // ← 今月のいいね合計

  // ── 更新日 ───────────────────────────────────────────────
  updatedAt: '2026-03-27',     // ← 更新したら必ずここも書き換える
};
